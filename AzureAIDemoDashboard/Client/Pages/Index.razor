@inject CultureHelper _cultureHelper
@page "/"

<PageTitle>Azure AI Dashboard</PageTitle>

<Speech CurrentCulture="_currentCulture"/>

@code {
    private CultureInfo _currentCulture = null!;

    protected override async Task OnInitializedAsync()
    {
        _currentCulture = await _cultureHelper.GetCultureAsync();
    }

}
